name: Unit Tests

on:
  pull_request:
  push:
  schedule:
    - cron: '0 3 * * *'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      cases: ${{ steps.cases.outputs.cases }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Set up Python
        uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6.0.0
        with:
          python-version: "3.11"
      - name: Install test runner
        run: python -m pip install --constraint requirements/requirements.dev.txt pytest pyyaml
      - name: Generate unit matrix
        id: cases
        run: |
          cases=$(
            python -bb -m pytest --list-cases |
              jq -ce '[.[] | select(.group == "unit") | {id, python}]
                | if length == 4 then . else error("expected four unit cases") end'
          )
          echo "cases=$cases" >> "$GITHUB_OUTPUT"

  test:
    needs: setup
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        case: ${{ fromJSON(needs.setup.outputs.cases) }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Set up Python
        uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6.0.0
        with:
          python-version: "3.11"
          cache: pip
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
      - name: Install test runner
        run: python -m pip install --constraint requirements/requirements.dev.txt pytest pyyaml
      - name: Run ${{ matrix.case.id }}
        run: |
          mkdir -p test-results
          python -bb -m pytest \
            --case="${{ matrix.case.id }}" \
            -v --tb=short \
            --junitxml="test-results/${{ matrix.case.id }}.xml"
      - name: Upload test output
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: tests-${{ matrix.case.id }}
          path: |
            test-results/
            .test-logs/
          if-no-files-found: ignore
